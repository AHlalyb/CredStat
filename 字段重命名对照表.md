# 服务器磁盘信息表字段重命名对照表

## 1. 字段映射关系

| 原字段名 | 新字段名 | 数据类型 | 约束条件 | 备注 |
|---------|---------|---------|---------|------|
| id | id | INT | AUTO_INCREMENT PRIMARY KEY | 主键ID，保持不变 |
| server_cred_id | server_cred_id | INT | NOT NULL | 服务器ID，保持不变 |
| os_type | server_cred_volu_os_type | ENUM('windows', 'linux') | NOT NULL | 操作系统类型 |
| windows_drive_letter | server_cred_volu_windows_drive_letter | VARCHAR(10) | DEFAULT NULL | Windows盘符号 |
| linux_device_name | server_cred_volu_linux_device_name | VARCHAR(50) | DEFAULT NULL | Linux设备名称 |
| linux_mount_point | server_cred_volu_linux_mount_point | VARCHAR(100) | DEFAULT NULL | Linux挂载点 |
| capacity_gb | server_cred_volu_capacity | VARCHAR(20) | NOT NULL | 容量（完整字符串） |
| used_space_gb | server_cred_volu_used_space | VARCHAR(20) | DEFAULT NULL | 已使用空间（完整字符串） |
| file_system_type | server_cred_volu_file_system_type | VARCHAR(50) | DEFAULT NULL | 文件系统类型 |
| notes | server_cred_volu_notes | TEXT | DEFAULT NULL | 磁盘信息备注 |
| created_at | server_cred_volu_created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | server_cred_volu_updated_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |
| created_by | server_cred_volu_created_by | VARCHAR(100) | NOT NULL | 创建人 |
| updated_by | server_cred_volu_updated_by | VARCHAR(100) | DEFAULT NULL | 更新人 |

## 2. 命名规范

本次字段重命名遵循以下命名规范：

### 2.1 命名规则
- **统一前缀**：所有字段使用`server_cred_volu_`作为统一前缀
- **驼峰命名**：使用下划线分隔单词，如`server_cred_volu_windows_drive_letter`
- **清晰描述**：字段名称准确反映字段内容和用途
- **一致性**：保持与系统其他表字段命名风格一致

### 2.2 设计考量

1. **前缀统一**：
   - 使用`server_cred_volu_`前缀明确标识该字段属于服务器磁盘信息表
   - 避免与其他表字段重名，提高代码可读性和可维护性
   - 便于数据库查询和管理，减少字段冲突风险

2. **清晰描述**：
   - 字段名称直接反映字段内容，如`server_cred_volu_capacity`明确表示服务器磁盘容量
   - 避免使用缩写，提高代码自解释性
   - 保持字段名称与业务逻辑的一致性

3. **扩展性**：
   - 统一命名规则便于后续添加新字段
   - 清晰的命名结构支持系统功能扩展
   - 便于与其他系统集成和数据迁移

4. **可读性**：
   - 统一命名规则提高代码可读性
   - 便于开发人员理解和维护代码
   - 减少因字段名称歧义导致的错误

## 3. 影响范围

### 3.1 数据库修改
- 执行`rename_volu_fields.sql`脚本修改表字段名称
- 修改前需备份`server_cred_volu_info`表数据
- 修改后需验证数据完整性

### 3.2 代码修改
- `save_server_cred.php`：修改INSERT语句中的字段名
- `create_server_cred_volu_info_table.sql`：更新表创建语句
- 前端代码：如后续有直接查询该表的代码，需同步更新

### 3.3 文档修改
- 更新数据库设计文档
- 更新API文档
- 更新相关技术文档

## 4. 执行步骤

1. **备份数据**：执行数据备份，确保数据安全
2. **执行SQL脚本**：运行`rename_volu_fields.sql`修改字段名称
3. **修改代码**：更新所有引用旧字段名的代码
4. **测试**：进行全面测试，确保系统功能正常
5. **更新文档**：同步更新相关文档
6. **部署**：部署修改后的代码和数据库

## 5. 注意事项

- 执行SQL修改前务必备份数据
- 修改代码时需全面搜索所有引用旧字段名的地方
- 测试时需覆盖所有相关功能
- 确保修改后的代码与数据库结构完全匹配
- 更新所有相关文档，确保文档与代码一致

## 6. 验证标准

- 所有数据库操作正常运行，无字段引用错误
- 数据读写逻辑保持原有功能不变
- 代码注释同步更新以反映字段名称变更
- 相关文档进行相应更新
- 系统功能测试通过
- 数据完整性验证通过
